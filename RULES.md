# 项目开发规则 (RULES.md)

## 设备规格

### 硬件要求
- **操作系统**: Android 14 
- **屏幕分辨率**: 800x480（横屏）
- **显示密度**: 160 dpi（1dp = 1px）
- **内存**: 3GB 及以上
- **存储空间**: 16GB 及以上
- **处理器**: 最低主频 2.5GHz

### 权限要求
- 存储权限（数据保存）
- USB通信权限（如需要）

## 开发要求

### 技术栈
- **开发语言**: Java（不使用 Lambda 表达式）
- **Android SDK**: API Level 29 (Android 10) 及以上
- **数据存储**: 原生 SQLite（不使用 Room）
- **第三方库**:
  - 网络请求: OkHttp 4.9.0
  - JSON解析: Gson 2.8.6

### 性能要求
- **启动时间**: 冷启动 ≤ 3秒
- **内存使用**: 峰值 ≤ 500MB
- **LED刷新率**: ≥ 60Hz
- **响应时间**: UI操作响应 ≤ 100ms
- **帧率**: 稳定保持 30FPS 以上

### 代码规范

#### 命名规范
- 类名: PascalCase (如 `AttendanceManager`)
- 方法名: camelCase (如 `processAttendance`)
- 常量: UPPER_SNAKE_CASE (如 `MAX_RETRY_COUNT`)
- 变量名: camelCase (如 `userCount`)
- 包名: 全小写 (如 `com.baidu.idl.face`)
- 资源文件: 小写+下划线 (如 `ic_launcher.png`)

#### 代码格式
- 缩进: 4个空格
- 最大行长: 120字符
- 花括号: 新行开始
- 空行: 方法间一个空行
- 导包顺序: Android > Java > 第三方 > 项目内

#### 注释规范
- 所有公共方法必须有JavaDoc注释
- 复杂逻辑必须有行内注释
- 异常处理必须有注释说明
- TODO注释必须包含处理人和时间

#### 异常处理
- 所有网络请求必须有超时和重试机制
- 数据库操作必须在try-catch块中
- LED控制异常必须有恢复机制
- 必须记录关键异常日志

### 安全规范
 
- **日志处理**:
  - 生产环境禁止打印敏感信息
  - 错误日志需脱敏处理
 

### UI规范
- 适配 800x480 分辨率
- 支持横屏显示
- 界面元素间距统一（8dp基准）
- 错误提示友好明确
- LED显示状态必须有可视化反馈

### 测试要求
- **单元测试**:
  - 核心业务逻辑覆盖率 > 80%
  - LED控制模块覆盖率 > 90%
- **集成测试**:
  - 完整的UI自动化测试
  - LED显示功能测试用例
- **兼容性测试**:
  - Android 10-13 系统兼容性
  - 不同分辨率适配测试
  - 弱网环境测试

### 代码审查
- 提交前自测通过
- 至少一名审查人确认
- 确保符合所有规范要求
- CI构建通过

## 部署要求

### 环境配置
- SDK授权文件正确配置
- LED控制模块正确初始化
- 模型文件完整部署
- 数据库初始化完成
- 网络参数正确设置

### 发布检查
- 代码混淆和签名
- 权限配置正确
- 资源文件完整
- 版本号正确设置
- LED控制模块测试通过

### 版本管理
- 遵循语义化版本 (Semantic Versioning)
- 保持更新日志完整性
- 标记重要版本节点

---

**注意**: 
1. 本规则文档为项目开发的基础规范，所有开发人员必须严格遵守
2. 如有疑问或需要修改，请及时沟通确认
3. 定期review和更新规范内容 